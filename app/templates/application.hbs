<div class="relative min-h-screen flex">
  <div class="flex flex-col w-64 h-screen px-4 py-8 bg-dark border-r border-light">
    <h2 class="text-xl font-semibold text-light text-center">Transmission Admin</h2>

    <div class="flex flex-col justify-between flex-1 mt-6">
      <nav>
        <MenuItem
          @route="torrents"
          @name="Torrents"
          @icon="swap-vertical"
        />
        <hr class="my-6 border-secondary">
        <MenuItem
          @route="settings"
          @name="Settings"
          @icon="cog"
        />
      </nav>
    </div>
  </div>

  <div class="flex-1">
    {{outlet}}
  </div>
</div>
